import React from 'react';

const Footer = () => {
    return (
        <footer className="w-full py-6 px-6 border-t border-slate-100 dark:border-slate-800 transition-colors duration-300 opacity-0">
            {/* Footer information removed as per request */}
        </footer>
    );
};

export default Footer;
